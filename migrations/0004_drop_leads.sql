-- Remove a tabela leads: criada na 0001 para a tela de leads da v1.1,
-- que foi removida por decisão de escopo (o produto não é CRM).
-- Nenhum código referencia esta tabela.
DROP TABLE IF EXISTS leads;
